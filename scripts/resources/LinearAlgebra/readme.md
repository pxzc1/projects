# Usage
- To run cell, run these commands:
    1. nix develop --impure
    2. source <env>/bin/activate (in this case it's '3.10.15')
    3. run vscode 'code' (shell alias of vscode)
    4. run cells
