# Usage
- To run cell, run these commands:
    1. nix develop (for first use or create a new environment)
    2. nix-shell --impure
    3. source <env>/bin/activate (in this case it's '3.10.15')
    4. run vscode 'code' (shell alias of vscode)
    5. run cells
